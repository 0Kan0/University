#include <iostream>
#include <cmath>
using namespace std;
int main (){

float x, raiz ;
float raiz_x=sqrt(x);
cout<<"Introduzca un numero positivo para hallar su raiz:"<<endl;
cin>>x;
if (x<0){

cout<<"Porfavor, introduzca un numero positivo"<<endl;
}
if (x>0){

cout <<"Su raiz es:"<<sqrt(x)<<endl;
 }
cin.ignore();
cin.get();

 }